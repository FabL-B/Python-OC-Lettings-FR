from django.apps import AppConfig

"""
Configuration module for the Profiles application.

This module configures the Profiles application within the Django project.
"""


class ProfilesConfig(AppConfig):
    """
    Configuration for the Profiles application.

    Attributes:
        name (str): Name of the application.
    """
    name = 'profiles'
